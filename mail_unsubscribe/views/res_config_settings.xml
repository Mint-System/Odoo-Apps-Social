<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">mail_unsubscribe.res_config_settings_view_form</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="mail.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//block[@id='discuss']" position="inside">
                <setting help="Before sending a message, all subscribers except for the current user will be removed.">
                    <field name="ignore_current_partner" />
                </setting>
                <setting help="Enable if message receivers should be subscribed automatically after sending a message.">
                    <field name="mail_post_autofollow" />
                </setting>
                <setting help="Enable if the current user should not be subscribed after sending a message.">
                    <field name="mail_create_nosubscribe" />
                </setting>
            </xpath>
        </field>
    </record>
</odoo>


